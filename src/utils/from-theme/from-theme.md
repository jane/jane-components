`import { fromTheme } from 'jane-components'` 

Pull values off the `theme` you've passed to the Styled Components
`ThemeProvider`. Example (in a Styled Component): `background-color: ${fromTheme('bg')};`